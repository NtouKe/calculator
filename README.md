# Calculator

For the Odin Project.
The goal is to build a functional on-screen calculator using HTML, CSS, and JavaScript. It should perform basic arithmetic operations (add, subtract, multiply, divide) and handle user interaction through buttons and keyboard input.
